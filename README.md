# BebodhCrawler

```
Database:
	-> run advance query in database server


Proxy:
	-> Check existing active proxies periodically
	-> Filter out active proxies when running proxy retriever
	-> New API endpoint for :
			1. A Random active proxy
			2. Sort used active proxy by last used
			3. What to do when all proxies have been used less than the minimum wait period
			4. Filter by:
						i) 
						
	-> API for updating proxy status, last used, last used status, blocked by domain
	-> 


Functionalities:
	-> Store crawl results in database
	-> Crawl Progress implementation
	-> 

```

